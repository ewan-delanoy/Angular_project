import { FrenchdatetimePipe } from './frenchdatetime.pipe';

describe('FrenchdatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchdatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
