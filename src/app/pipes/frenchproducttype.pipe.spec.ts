import { FrenchproducttypePipe } from './frenchproducttype.pipe';

describe('FrenchproducttypePipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchproducttypePipe();
    expect(pipe).toBeTruthy();
  });
});
