import { FrenchorderstatePipe } from './frenchorderstate.pipe';

describe('FrenchorderstatePipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchorderstatePipe();
    expect(pipe).toBeTruthy();
  });
});
