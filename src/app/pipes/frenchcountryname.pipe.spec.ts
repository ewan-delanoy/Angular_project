import { FrenchcountrynamePipe } from './frenchcountryname.pipe';

describe('FrenchcountrynamePipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchcountrynamePipe();
    expect(pipe).toBeTruthy();
  });
});
